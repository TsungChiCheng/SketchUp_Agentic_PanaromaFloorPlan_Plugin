services:
  backend:
    restart: unless-stopped

  depth-service:
    restart: unless-stopped
    environment:
      DEPTH_DEVICE: cuda
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    gpus: all
